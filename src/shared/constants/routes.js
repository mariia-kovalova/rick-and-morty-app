export const ROUTES = {
  basename: '/rick-and-morty-app',
  home: '/',
  characters: 'characters',
  locations: 'locations',
  episodes: 'episodes',
  library: 'library',
  all: '*',
};

export const { basename, home, characters, locations, episodes, library, all } =
  ROUTES;
